import React from "react";
import PageContent from "./../../UI/Navbars/Primary/Primary";
import paths from "./paths";

const Subject = () => {
  return (
    <React.Fragment>
      <PageContent tabs={paths} />
    </React.Fragment>
  );
};

export default Subject;
