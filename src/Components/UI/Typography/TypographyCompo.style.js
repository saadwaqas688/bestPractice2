import { styled } from "@mui/system";
import Typography from "@mui/material/Typography";

export const Heading6 = styled(Typography)(({ theme }) => ({
  // fontSize: "15px",
  // color: "#31243D",
  // fontWeight: 600,
  fontFamily: "AvenirNext",
}));
